// credentials.h.example - Template for local credentials
// Copy this file to credentials.h and fill in your actual credentials
// credentials.h is gitignored and will not be committed

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Configuration (for development builds)
#define WIFI_SSID "your_wifi_ssid"
#define WIFI_PASS "your_wifi_password"

// MQTT Configuration
#define MQTT_SERVER "mqtt.example.com"
#define MQTT_PORT 1883
#define MQTT_USER "your_mqtt_username"
#define MQTT_PASS "your_mqtt_password"

#endif
